{
  "name": "vocabulary-ai-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Local proxy for Google Imagen 4 and Gemini Flash Image Preview to avoid CORS and keep API keys server-side",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
